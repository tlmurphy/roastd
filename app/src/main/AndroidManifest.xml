<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="net.jmhossler.roastd">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:fullBackupContent="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".maintask.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".profiletask.ProfileActivity" />
    <activity android:name=".searchtask.SearchActivity"
      android:theme="@style/Theme.AppCompat.Light.NoActionBar">
      <meta-data android:name="android.app.searchable"
        android:resource="@layout/searchable" />
    </activity>
    <activity android:name=".favoritestask.FavoritesActivity" />
    <activity
      android:name=".logintask.LoginActivity"
      android:theme="@style/AppTheme.NoActionBar" />

    <meta-data
      android:name="preloaded_fonts"
      android:resource="@array/preloaded_fonts" />

    <activity android:name=".applicationtask.ApplicationActivity" />
    <activity android:name=".beantask.BeanActivity" />
    <activity android:name=".drinktask.DrinkActivity" />
    <activity android:name=".shoptask.ShopActivity"></activity>
  </application>

</manifest>
